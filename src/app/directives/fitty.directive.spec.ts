import { FittyDirective } from './fitty.directive';

describe('FittyDirective', () => {
  it('should create an instance', () => {
    const directive = new FittyDirective();
    expect(directive).toBeTruthy();
  });
});
